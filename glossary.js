/* non-exhaustive list 
    - Concepts to know 
    - Concepts good to know
        - Concepts really good to know
        - Concepts good to know
*/

/* Concepts to know */

// REACTO => problem solving and coding are two different things (Remember!!)
// indentation and elegant code
// variables => var, let, const
// functions
// loops
// console.log() vs return
// scope
// coercion => implicit and explicit
// truthy values => !!, !, Boolean(), if(){}else{}, &&, ||
// falsey values => !!, !, Boolean(), if(){}else{}, &&, ||
// methods vs functions (high level understanding)
// strings
// String.split("");
// String.includes();
// Math.min();
// Math.max();
// Array.sort();
// Object.values();
// numbers
// booleans
// arrays => flat and multidimensional
// objects => flat and multidimensional
// recursion => print, return, strings, numbers, arrays (flat and multidimensional), objects(multidimensional), working mixed and highly nested input

/* Concepts good to know but not required */

/* really good to know */
Array.forEach();
Array.map();
Array.reduce();
Math.abs();
Math.sqrt();
Math.pow();
Number.isNaN();
// spread operator ...
// creating custom methods on custom objects

/* good to know  */
Math.random();
String.substring();
Number.isInteger();
Number.parseFloat();
Number.parseInt();
Number.toFixed();
Infinity - Infinity;
// + operator
// prototype inheritance
// classes
//
